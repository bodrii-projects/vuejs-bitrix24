Запуск контейнер и попадаем ему в консоль
docker run -it

Задача: Не только запустить сервер но что бы и
1. Создаем образ
    docker build .
2. Запустить образ и войти в его терминал
    docker run -p 3000:3000 -it --rm 4d5bcb390cd3 /bin/sh
3.


* Командо ниже по идеи я могу запускать процесс сборки проекта и другие команды
docker exec -it <id контейнера> sh - Запускает дополнительный процесс sh в работающем контейнере.
